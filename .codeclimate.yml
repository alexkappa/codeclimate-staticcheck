---
engines:
  staticcheck:
    enabled: true
    config:
      package_dir: github.com/alexkappa/codeclimate-staticcheck
  # fixme:
  #   enabled: true
  # gofmt:
  #   enabled: true
  # golint:
  #   enabled: true
  #   exclude_paths:
  #     - "**_test.go"
  # govet:
  #   enabled: true
ratings:
  paths:
    - "**.go"
exclude_paths:
  - "vendor/"
  - ".wercker/"
